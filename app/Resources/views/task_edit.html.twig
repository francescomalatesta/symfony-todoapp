{% extends 'layout/master.html.twig' %}

{% block title %}Task Update - {% endblock %}

{% block prebody %}
    {% include 'includes/menu.html.twig' %}
{% endblock %}

{% block contents %}

    <h2>Task Update</h2>

    <hr>

    <form action="{{ path('task_post_edit', { taskId : task.id }) }}" method="post">
        <p>
            <label for="title">Title:</label>
            <input type="text" name="title" class="form-control" value="{{ task.title }}" autofocus />
        </p>
        <p>
            <label for="description">Description:</label>
            <input type="text" name="description" class="form-control" value="{{ task.description }}" />
        </p>

        <hr>

        <p>
            <button class="btn btn-success form-control">Update Task</button>
        </p>
    </form>
{% endblock %}